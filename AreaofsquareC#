using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
      int side=10;
        int area=side*side;
        Console.WriteLine($"Area of the square with  side {side} and area is {area}");
    }
  }
}
